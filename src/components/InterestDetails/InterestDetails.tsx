import styled from 'styled-components';

interface IProps {}

function InterestDetails({}: IProps) {
  return <StyledInterestDetails>InterestDetails</StyledInterestDetails>;
}

const StyledInterestDetails = styled.div`
  overflow: hidden;
`;

export default InterestDetails;
