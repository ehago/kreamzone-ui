import styled from 'styled-components';

interface IProps {}

function SellingDetails({}: IProps) {
  return <StyledSellingDetails>SellingDetails</StyledSellingDetails>;
}

const StyledSellingDetails = styled.div`
  overflow: hidden;
`;

export default SellingDetails;
