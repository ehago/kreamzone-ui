import styled from 'styled-components';

interface IProps {}

function WishDetails({}: IProps) {
  return <StyledWishDetails>WishDetails</StyledWishDetails>;
}

const StyledWishDetails = styled.div`
  overflow: hidden;
`;

export default WishDetails;
